diff --git a/dist/plugins/nodejs-compat.js b/dist/plugins/nodejs-compat.js
index a99cf51c2ae56218eada874d093f146e28616fc7..f3fed93ec708e5f562cc6323a2839bccac9456c2 100644
--- a/dist/plugins/nodejs-compat.js
+++ b/dist/plugins/nodejs-compat.js
@@ -23,7 +23,11 @@ function makeUnenvPlugin(options) {
         ],
     }).env;
     const injectVirtualModules = makeInjectVirtualModules(inject);
-    const require = createRequire(import.meta.url);
+    let require;
+    const getRequire = () => {
+        require ??= createRequire(import.meta.url);
+        return require;
+    };
     return [
         esmExternalRequirePlugin({
             external: [...external],
@@ -54,7 +58,7 @@ function makeUnenvPlugin(options) {
                     const resolved = await this.resolve(id, importer, options);
                     if (resolved)
                         return resolved;
-                    return { id: require.resolve(id) };
+                    return { id: getRequire().resolve(id) };
                 },
             },
         },
